import { View } from 'react-native';
import React from 'react';
import ThemedText from '@/theme/components/ThemedText';

const Profile = () => {
  return (
    <View>
      <ThemedText size="lg">Profile</ThemedText>
    </View>
  );
};

export default Profile;

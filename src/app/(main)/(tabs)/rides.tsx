import { View } from 'react-native';
import React from 'react';
import ThemedText from '@/theme/components/ThemedText';

const Rides = () => {
  return (
    <View>
      <ThemedText size="lg">Rides</ThemedText>
    </View>
  );
};

export default Rides;
